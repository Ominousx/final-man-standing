{
  "buildCommand": "cd vct-survivor-web && npm install && npm run build",
  "outputDirectory": "vct-survivor-web/.next",
  "framework": "nextjs",
  "installCommand": "cd vct-survivor-web && npm install",
  "devCommand": "cd vct-survivor-web && npm run dev",
  "regions": ["iad1"],
  "functions": {
    "vct-survivor-web/app/**/*.tsx": {
      "maxDuration": 30
    }
  }
}
